FROM node:20-alpine

WORKDIR /app

# Install dependencies first for layer caching
COPY package.json package-lock.json* ./
RUN npm install

EXPOSE 5173

# Vite dev server with host access
CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0"]